# 2026-04-25 工作日志

## 首页与 About 页面统一风格 + 删除 Contact

- 将 `index.md` 改造为与 `about.md` 相同的 hero 卡片布局，复用 `site.data.about` 数据和 `about.css` 样式，删除了原有的纯 Markdown 文本和快速链接列表。
- 删除了 `about.md` 底部的 Contact section（`<section class="card about-panel">` 整块）。
- 删除了 `about.css` 中的 `.about-contact-list` 样式规则。
- 修复了已有的 Jekyll 构建问题：`jekyll-theme-primer` 的 SCSS 文件含 UTF-8 字符，旧版 sass gem（3.7.4）默认以 US-ASCII 解析导致报错。解决方式：构建时设置环境变量 `RUBYOPT="-EUTF-8"`。
- 在 `_config.yml` 中添加了 `sass: style: compressed` 配置。

## 全站去边框化（参照 lyushisyan.github.io 风格）

- 用户选择"方案B（激进）"：全面去边框，靠间距和背景色区分层次。
- `style.css` 改动：
  - `.card`：`border: 1px solid` → `border: none`
  - `.item-list li`：去除左侧 `border-left: 3px solid`，padding-left 清零
  - `.software-logo`：去 border
  - `.publication-preview`：去 border
  - `.publication-tag`：去 border，背景改为 `#f3f4f6`
  - `.publication-detail`：去虚线 border，背景改为 `#f3f4f6`
  - `.teaching-meta-chip`：去 border，背景改为 `#f3f4f6`
  - `.blog-post-item`：去 border 和 box-shadow，hover 改为背景色变化（`#f8fafc`）
- `about.css` 改动：
  - `.about-avatar`：去 border
  - `.about-timeline li`：去 `border-left: 2px solid`，padding-left 清零
  - `.about-pub-item`：去 border，背景改为 `#f8fafc`
  - `.about-software-logo`：去 border

## Publications/Software 图片加方角边框 + Blog 目录式布局

- `publication-preview`：`border-radius: 10px` → `4px`，补回 `border: 1px solid var(--line)`（方角有框）
- `software-logo` / `about-software-logo`：同上，方角有框
- Blog 每条改为目录式：
  - 第一行：`<a 标题>` + CSS dotted 点线填充 (`.blog-post-toc-dots`) + 日期，靠 flexbox 实现
  - 第二行：tags chips + `Read article →` 紧跟在同行
  - HTML 新类名：`.blog-post-toc-row`、`.blog-post-toc-title`、`.blog-post-toc-dots`、`.blog-post-toc-date`
  - 卡片 padding 从 `0.9rem 1rem` 缩小到 `0.55rem 0.2rem`（更紧凑）
- 构建命令：`RUBYOPT="-EUTF-8" bundle exec jekyll build`（解决 SCSS UTF-8 问题）



- 顶部 tag cloud chip 变大变好看：字号 0.82rem→0.93rem，padding 加大，加 `font-weight: 500`，hover 改为蓝色软背景，active 保持蓝底白字并加阴影，count 数字改为小圆角胶囊背景。
- 每条文章卡片：删除了 excerpt 简介段落和底部 tags 列表，只保留日期 + 标题 + Read article 按钮。
- 清理了 `.blog-post-excerpt`、`.blog-post-tags`、`.blog-post-tag` 等废弃 CSS 规则。
- 后续补充：在 Read article 按钮左侧加回了文章 tag 标签（可点击过滤），删除的只有 excerpt 简介。
